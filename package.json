{
  "name": "projectflow-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for ProjectFlow",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install",
    "build": "npm run build -w frontend",
    "start": "npm start -w backend",
    "dev": "npm run dev -w backend"
  },
  "engines": {
    "node": ">=20"
  }
}
